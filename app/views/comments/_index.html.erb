<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
        <p><%= comment.content %></p>
        <p>投稿者：<%#= comment.user.email %></p>
        <p><%#= link_to 'コメントを削除する',comment_path(comment), method: :delete, remote: true %></p>
      </li>
    <% end %>
  <% end %>
</ul>